# FastAPI后端依赖

# Web框架
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0

# HTTP客户端
httpx==0.25.1

# 数据处理
pandas==2.1.3
numpy==1.26.2
matplotlib==3.8.2  # 用于生成电压曲线图

# 深度学习（GAN模型）
torch>=2.0.0  # PyTorch，用于加载GAN模型（可选，如果模型未使用PyTorch可移除）

# 数据验证
pydantic==2.5.0

# 报告生成
jinja2==3.1.2
weasyprint==60.1  # PDF生成（可选）
reportlab==4.0.7  # PDF生成库

# 数据库
# SQLite已内置，无需额外安装

# 日志
python-json-logger==2.0.7

# 其他工具
python-dotenv==1.0.0
psutil==5.9.6  # 系统监控

